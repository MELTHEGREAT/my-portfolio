Drop your project screenshots here, named to match the `image` paths in
src/data/projects.js:

  adventure-resort.png
  duyanan-kiosk.png
  it-support.png

If an image is missing, the project card shows a plain placeholder instead
of a broken image.
